apiVersion: v1
kind: Pod
metadata:
  name: network-test-pod
spec:
  containers:
  - name: network-test-container
    image: ubuntu:latest  # Use an image that includes curl
    command: ["sleep", "3600"]
